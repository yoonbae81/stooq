# Tests directory for captcha recognition

This directory contains test samples and test scripts for the captcha recognition functionality.

## Running Tests

```bash
python tests/test_captcha.py
```

## Test Samples

The `samples/` directory contains captcha images from stooq.com. The filename (without extension) represents the correct answer for each captcha.
